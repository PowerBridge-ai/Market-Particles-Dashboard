{
  "buildCommand": "uv pip install -r requirements.txt && next build",
  "framework": "nextjs",
  "installCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && uv pip install -r requirements.txt",
  "outputDirectory": ".next",
  "regions": ["sfo1"],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "build": {
    "env": {
      "NEXT_TELEMETRY_DISABLED": "1"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "github": {
    "silent": true
  }
} 